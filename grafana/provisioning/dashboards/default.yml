apiVersion: 1

providers:
  - name: TalentNest
    orgId: 1
    folder: TalentNest
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/dashboards
