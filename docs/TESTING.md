# Testing

## Current State
- Currently relying on manual testing and TypeScript compiler checks.

## Future Plans

### API Testing
- Implement `Vitest` and `Supertest` for backend route testing.
- Write tests for Auth flow, JWT validation, and CRUD operations.

### Integration Testing
- Use `React Testing Library` for component testing.

### E2E Testing
- Implement `Playwright` or `Cypress` for critical user journeys (Signup -> Onboarding -> Post Job).
